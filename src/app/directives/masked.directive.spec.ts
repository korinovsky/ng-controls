import {MaskedDirective} from './masked.directive';

describe('MaskedDirective', () => {
    it('should create an instance', () => {
        const directive = new MaskedDirective(null, null, null);
        expect(directive).toBeTruthy();
    });
});
