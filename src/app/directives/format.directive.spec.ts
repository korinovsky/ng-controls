import {FormatDirective} from './format.directive';

describe('FormatDirective', () => {
    it('should create an instance', () => {
        const directive = new FormatDirective(null, null, null);
        expect(directive).toBeTruthy();
    });
});
