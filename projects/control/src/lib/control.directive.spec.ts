import {ControlDirective} from './control.directive';

describe('ControlDirective', () => {
    it('should create an instance', () => {
        const directive = new ControlDirective(null, null, null);
        expect(directive).toBeTruthy();
    });
});
